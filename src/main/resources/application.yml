spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/phonebook
    username: admin
    password: 18000000


scheduled:
     contactRepository: 600000

github:
  baseUrl:
       "https://api.github.com/users/"
  path: "/repos"
  maxAttempts: 5
  waitDuration: 5
  retry: "github"
